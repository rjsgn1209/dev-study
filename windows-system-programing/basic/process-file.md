# 프로세스와 파일 개념

프로세스(스레드)는 주체이고 파일은 대상체(객체)이다
  
프로세스에는 ID, Handle이 존재하는데 ID는 단순 식별정보 이고 Handle은 직접 제어를 위한 값이다.
  
프로세스(주체)는 파일(객체)에 접근하여 제어하는데 제어란 읽기, 쓰기, 실행으로 나누어 진다.
  
핸들은 결국 포인터이고 토큰 또한 포인터이며 일반적으로 해당 포인터를 따라갔을때 
구조체에 정보들이 닮겨있는 경우 토큰이라 부르는 경우가 많다.
